{
  "name": "v2ex-mirror",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "sync": "node scripts/fetch/run.mjs",
    "backfill": "node scripts/fetch/backfill.mjs",
    "build": "node scripts/build/run.mjs",
    "sync:build": "npm run sync && npm run build",
    "backfill:build": "npm run backfill && npm run build"
  }
}
